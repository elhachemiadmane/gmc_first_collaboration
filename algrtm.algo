ALGORITHM algorithm_name
VAR
//declartion des variables 
    a, b, c : INTEGER;
    s: INTEGER;
    d: INTEGER;
BEGIN
    read (a);
    read (b);
    read (C);  
    s:= a+b+c;
    write (s);
    counter01_01: INTEGER;
    //for teste
    read(a);
/* dddddddddddddd */

END